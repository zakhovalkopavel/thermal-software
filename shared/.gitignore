# But keep directories we manage
!/

# Allow these directories and their contents
!/sources/
!/processed/

# Ignore everything inside except .gitkeep (subfolders will override if needed)
/processed/*

# Keep gitkeep files
!/sources/.gitkeep
!/processed/.gitkeep

